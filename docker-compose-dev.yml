version: "3.9"
services:
  self-astro:
    image: self-astro
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    volumes:
      - type: bind
        source: .
        target: /usr/src/app/
      - type: volume
        target: /usr/src/app/node_modules
    networks:
      - frontend
    ports:
      - 3000:3000
networks:
  frontend:  